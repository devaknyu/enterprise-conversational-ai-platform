# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.6

# Config management
pydantic-settings==2.3.4

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Structured logging
structlog==24.4.0

# HTTP client + retry logic
httpx==0.27.2
tenacity==8.5.0

# Vector DB
chromadb==0.5.23

# LLM — dev/free path
# Gemini 2.0 Flash via AI Studio (no billing account required)
google-generativeai==0.8.3

# LLM — production path
# Gemini 1.5 Pro via Vertex AI (IAM auth, data residency)
# Also provides Vertex AI embedding API used in production
google-cloud-aiplatform==1.67.1
